var age = 24;

if(age >= 18){                      //Gets the number value stored in age and makes the comparison
    console.log("You can vote.");   //If the comparison results in a true it prints this
}else{
    console.log("You can't vote");  //If not, then it prints this
}