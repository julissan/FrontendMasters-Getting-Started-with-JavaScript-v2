var name = "Juan David"; //Created the variable name and assigned the value "Juan David" to it (string type).

var age; //Created the variable age, undefined type at the moment.
age = 24; //Assigned 24 to variable age, now its number type.

var friends = ["Brayan", "Felipe", "Diego"]; //Created friends array and assigned values to the first 3 positions (0, 1 and 2).


console.log(friends.length); //Print the size of the friend array.
console.log(friends[1]); //Print the value in the 1 position of the friends array.