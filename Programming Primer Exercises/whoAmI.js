var student = "Juan David Lis Santofimio";
var githubUsername = "julissan";
var age = 24; //Created 3 variables and assigned values to them.

function whoAmI(myName, myUsername, myAge){
    console.log("Hi, I'm " + myName + ", my username on github is " + myUsername + " and I'm " + myAge + " years old.")
    //Inside the console.log there are multiple string concatenations.
}

whoAmI(student, githubUsername, age); //Call the function whoAmI giving the three variables as parameters.